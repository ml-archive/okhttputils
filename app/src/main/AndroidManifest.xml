<manifest package="dk.nodes.okhttputils"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".error.HttpErrorActivity"
            android:screenOrientation="portrait"
            android:theme="@style/TransparentActivity"
            android:windowSoftInputMode="adjustResize" />
        <provider
            android:authorities="${applicationId}.HttpErrorContentProvider"
            android:name=".error.HttpErrorInitProvider"
            android:exported="false" />
    </application>
</manifest>
